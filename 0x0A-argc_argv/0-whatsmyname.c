#include <stdio.h>

/**
 * main - prints name
 * @argc: num of argument
 * @argv: array with argument
 *
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}

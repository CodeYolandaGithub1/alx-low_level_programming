#include "main.h"

/**
 * print_last_digit - prints the last digit of a num
 * @n: num last digit to be printed
 * Return: value of the last digit
 */

int print_last_digit(int n)
{
	int i;

	i = n % 10;

	if (n < 0)
	n = -n;

	if (a < 0)
	a = -a;

	_putchar(i + '0');

	return (i);
}
